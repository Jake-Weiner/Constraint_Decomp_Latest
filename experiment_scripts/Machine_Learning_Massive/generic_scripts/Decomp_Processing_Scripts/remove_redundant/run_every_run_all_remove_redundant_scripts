#!/bin/bash

# this script will iterate through every folder and run the run_all bash script for each test
cd Greedy
cd redundant
./run_all_remove_redundant_constraints
cd ..
cd ..
cd NSGA
cd redundant
./run_all_remove_redundant_constraints

